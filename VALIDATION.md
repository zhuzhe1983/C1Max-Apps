# 2026-09-24：私有默认服务器配置与最近应用更新验证

StreamPlayer 与 CrossPoint 的默认地址/账号移入本地 `config/default-servers.local.json`，Git 忽略且文件权限 0600；仓库只保留空白 example 与说明，删除测试中的个人 OPDS 地址。部署脚本先验证配置 schema/字段/URL，再在公开应用包之外单独同步到 `/storage/apps/data/default-servers.json`（0600）。默认文件不进入源码摘要、catalog 或 release payload；该文件缺席时不改变设备默认值。应用已有登录/服务器设置优先，默认值只预填新安装，不自动登录或导入访问令牌。

- Linux ASan/UBSan 验证真实 MediaClient 的默认值回退、已保存设置优先、令牌不导入、缺失/坏类型/超长/无效 URL 处理；部署侧 Python 验证 schema、允许字段、URL、大小和非法字符。Mac 首次尝试因 Linux 专用 sys/prctl.h 无法编译，改用 Linux 测试镜像通过；未以失败的平台构建冒充通过。
- 真机隔离数据没有各应用已有配置时，CrossPoint 读取私有默认值并打开实际 OPDS 搜索结果，StreamPlayer 的默认地址、用户名、遮挡密码正确预填。正式部署后设备默认配置 SHA-256 与本地一致；原有应用保存配置摘要不变。包含私有地址的 QA 截图/日志只留在本机忽略目录，没有加入文档或 Git。
- 完整 MIPS 构建通过。提交前复核 StreamPlayer 时间线/视频布局/HLS/本地字幕及异步字幕，终端解析器/PTY/字号尺寸，实体键盘，DOSBox 输入，PS1 视频和相机完整测试集；均通过。OPDS/EPUB/传输测试结果见下一条。本轮没有重新启动 PS1 游戏或媒体音频播放。
- 待提交文件及公开 payload 检查未包含本地服务器地址/主机名、设备访问令牌或私有默认配置；`.local.json` 已验证被 Git 忽略。公开示例保持空值，真实值仅在本机和设备 data 目录。

---

# 2026-09-24：CrossPoint 大 EPUB 崩溃修复、按章读取与字号调节

CrossPoint 0.3.1 已部署至 `/storage/apps/releases/20260924-205817-65db3bee`，143 文件、58,856,677 字节，逐文件 SHA-256 验证通过。中途 ADB 断开，用户唤醒后完成隔离测试、正式安装和正常 launcher 验证。相对本轮开始版本，除 manifest/catalog 外，仅 CrossPoint 二进制/README、共享音量辅助程序和 StreamPlayer 二进制变化；后者增加私有默认服务器读取。

- 用户第一本为 8,969,404 字节 EPUB，ZIP 完整性通过，1752 个成员、1743 个 spine 条目（排除一个 non-linear 项后可读顺序 1742 项），总解压内容 19,275,541 字节。旧读取器真机独立基准在 16.01 秒后 SIGSEGV，用户观察的“打不开”不是仅下载慢。将同一 OPF 的旧 spine 正则在 Linux ASan 下复现，明确报告 stack-overflow；旧路径还逐章 fork unzip 并累计整本文字，另有 4 MiB 总文字限制。第二本 AZW3 为 702,393 字节，走 libmobi，不能据此把所有 EPUB 与 AZW3 的速度一概比较。
- EPUB 换成原生 ZIP 索引、TinyXML-2 metadata/spine 和已有 miniz 解压，后台只读当前章，校验 CRC/成员大小/偏移。保留取消；W/S 换章，A/D 翻页，章节尾继续下一章。非线性内容跳过，无全书 4 MiB EPUB 文字上限；每章 2 MiB、压缩成员 8 MiB 等有界限制见 README。用户实际文件在电脑端可读取目录、序言和正文；索引约 5 ms、目录文字约 4 ms，这些为电脑基准，**不是词典耗时**。
- 字库以只读 mmap 加载，正文单独字体与缓存。Shift＋音量 ＋/− 将正文按 2 px 调至 14–32 px，保存至 reader.json；标题与控件字号不变。换字号以当前页首字符为锚，前后页面惰性重排，不扫描之前全部页面。共享音量辅助程序验证 CrossPoint 的存活进程标记后抑制该组合键，普通音量键保持原行为；终端原有快捷键仍保留。真机事件注入验证从 14 到 32 的边界、20/22/24 等中间字号、重开后记住字号；当前章节字节锚始终为 559。换字号后先前翻再后翻的正文像素与原页逐点相同，组合键前后 softvolume 完全相同；正式安装后再次测试加/减各一次，仍不改变音量。这是事件注入验证，不声称由用户亲按键帽。
- 同一 HTTPS 图书 URL 的 1 MiB Range 请求：电脑 20.992 s（49,951 B/s），词典 wall 8.67 s、下载约 124 KB/s，用户 CPU 0.25 s、系统 0.06 s。采样有明显波动，说明此次下载主要等网络/服务端，而非 EPUB 排版 CPU；不能据此断言服务器设置了限速。UI 补充平均速度与预计剩余秒数，减少 wget 进度日志，持续下载总超时从 3 分钟改为 30 分钟；12 秒网络读取超时和主动取消保持。未声称提高外部网络吞吐。
- 主机 ASan/UBSan：存储/Deflate ZIP、UTF-8 与百分号相对路径、namespace/spine 顺序与 linear=no、损坏后章不阻止前章读取、CRC/短包/加密/越界限制/取消，以及 1750 章回归通过。既有 OPDS/分页测试加上反向分页重组逐字节一致测试通过。Linux HTTP 测试验证 no-verbose 调整后重定向、认证作用域、完整文件、重复命名、短响应/HTML/超限拒绝与取消清理均通过。MIPS CrossPoint 与 volume 构建及 git diff --check 通过。

本轮证据与用户书籍副本仅在忽略目录 `.build/crosspoint-perf-qa/`，未打包。新版真机首次打开的读取/排版日志为 206/55 ms；正式安装为 189/47 ms（约 0.24 秒准备首屏，未计液晶扫描时间），序言与正文章节约 67–96 ms。正文与 AZW3 正常打开并截图；正文最大字号未超出阅读区。再次下载同一图书时界面显示 816 KiB / 8.6 MiB、114 KiB/s、约 71 秒，取消后隔离书架仍只有原两本，无临时残留。EPUB 进程 RSS 约 6.7 MiB，之后打开 AZW3 的进程 HWM 约 12.4 MiB（不含所有内核缓存）。两个正式书籍文件、OPDS 配置与 StreamPlayer 登录配置部署前后摘要完全相同；设备临时目录 `/tmp/c1max-crosspoint-perf` 已清理。原 EPUB SHA-256 为 `9c780cb8066c15a01a2d83cb901c11fb64ad018faf42bb5e3c5ab2aa0ec28f18`。

---

# 2026-09-24：CrossPoint OPDS 浏览、搜索与下载

已部署 `/storage/apps/releases/20260924-182752-9d857ddc`，143 个文件、58,902,061 字节，逐文件 SHA-256 校验通过。CrossPoint 版本为 0.3.0；相对上一次正式包，除 manifest/catalog 外只有 CrossPoint 二进制、README 和两份许可证变化。设备首个 OPDS 服务器已按用户指定地址配置为匿名访问；旧草稿备份到忽略目录后清除，服务器配置不随应用包发布。正常 launcher 已打开正式新版，实测返回 Calibre 首页九个分类入口。

- 参考 CrossPoint Reader `0f01106ad6e9eacdd107a04b9128d15cd0ef02e6` 的 OPDS parser/browser/filename/URL 路径，保留来源与 MIT 说明。Linux 端使用固定 TinyXML-2 11.0.0（未修改的 vendored 源码与 zlib 许可证），替换旧的正则 Atom 解析；支持 namespace、实体/CDATA、xml:base、feed 搜索/前后页链接、多个 acquisition 格式。按 MIME 保存扩展名，补充 Calibre AZW3 的 x-mobi8-ebook；不再把无后缀 PDF 链接保存成 EPUB。
- 保存的服务器根地址与当前浏览位置分离。列表显示六行并可浏览全部条目，A/D 翻屏，到当前响应边界继续取服务器页；返回上级恢复选择位置。F/触摸搜索支持 Calibre searchTerms 模板和字段查询。书籍详情选择 EPUB/AZW3/MOBI/PDF/TXT/Markdown 后下载，EPUB 优先。仅操作第一个服务器，保留其他保存记录；未实现 OPDS 2、OpenSearch 描述发现或封面加载。
- 后台线程执行网络请求，UI 显示连接与字节进度；返回/触摸取消/电源退出会结束并回收本应用的 wget。TLS 校验保持开启，手动跟随有限重定向，认证只发给配置服务器同源地址。下载以临时目录流式写盘，检查签名、fsync 后原子发布；同名添加序号，失败与取消不污染书架。目录上限 1 MiB/512 条，下载上限 128 MiB，历史 32 层。
- 主机 ASan/UBSan：真实 Calibre 首页九项和最新页五十项，以及命名空间、实体/CDATA、XML 截断、DTD 拒绝、空目录、URI 路径/查询、格式优先级、UTF-8 文件名和边界通过。两个本地 HTTP 服务实测重定向、同源认证与跨源剥离、精确文件、重复命名、短响应/HTML/超限拒绝、取消及临时文件清理；捕获并修复 wget 非 HTTP 的 Location 提示混入重定向地址问题。
- 真机隔离目录验证首页 7–9 项、最新页 49–50/50、继续服务器下一页、返回后恢复 49–50/50；键码注入搜索、右上退格、EPUB/AZW3 选择展示，实际下载一个 107,728 字节 EPUB。ZIP CRC 和容器元数据检查通过，打开并翻页成功。21.8 MiB PDF 下载在加载中取消，隔离目录只有之前完整 EPUB，没有 PDF 或临时残留。触摸 URL 后输入只修改 URL，名称不变，退格可恢复原地址。本轮是注入事件验证，不声称由用户亲按实体键帽。
- 发现原阅读器固定字数切页会截掉中文/多换行内容，改为按实际字体与 742×226 视口测量。逐页生成并缓存，避免启动时对整本书做昂贵排版；读到末尾才显示总页数。主机验证分页重组与原文逐字节相同、UTF-8 边界完整，真机前两页完整显示。原 EPUB/TXT/Kindle/PDF 文字提取路径保留；图片型 PDF/漫画仍不能作为整页图像阅读，格式页已明确提示。
- 近期正式候选进程 RSS 2,636 KiB、HWM 2,844 KiB；之前目录/下载功能短测 HWM 3,140 KiB。这些仅为主应用进程，不含 wget、字库页缓存和全部系统内存，也不代表大型书籍或长期性能。MIPS 构建、`git diff --check` 通过。

证据位于忽略目录 `.build/crosspoint-opds-qa/`：上游 commit、真实 XML、parser/transport 测试日志、目录/搜索/格式/下载/取消/输入/阅读/正式入口截图、ZIP CRC 摘要与部署日志。测试书籍只在隔离数据路径内，正式书架文件列表未变化；测试数据已从设备清理，证据留在本机忽略目录。设备停留在正常 launcher 打开的正式在线目录，匿名服务器配置已验证。

---

# 2026-09-24：取景状态标签移出画面

已部署 `/storage/apps/releases/20260924-175607-4c211eb0` 并逐文件校验。移除左上角状态底板，实时取景/启动/保存状态统一放在右侧标题下，分辨率挪到画幅按钮上方。MIPS camera 构建及 `git diff --check` 通过；真机 4:3、16:9 截图确认照片区域无遮挡，原始画幅设置和全部用户照片摘要保持不变。本次仅布局调整，没有新增单元测试；未触发快门或音频。证据在 `.build/camera-status-qa/`，设备临时文件已清理。

同轮只读检查未发现可用 IMU：无 IIO/sensors 设备，输入节点仅矩阵键盘、GPIO 按键和 CST3xx 触摸；I²C 注册设备与设备树未见加速度计/陀螺仪。不能凭软件枚举排除未启用或未接驱动的硬件。

---

# 2026-09-24：拍立得快门反馈与 imagegen 透明相框

已部署 `/storage/apps/releases/20260924-160833-ad779dc1`，141 个文件、58,771,545 字节，部署脚本逐文件校验通过。除 manifest/catalog 外，本轮运行内容仅 camera 二进制和七个相机资源发生变化。

- 使用内置 imagegen 生成白相纸、奶油纸、黑胶片三套真实 RGBA PNG；原始图保存在 `camera/assets/frames/`，完整提示词在 `camera/frame-prompts.json`。素材中心透明，保留纸纹、边缘与胶片孔 alpha；运行时使用分区映射保留角部形态，预览、选择缩略图和 JPEG 使用同一合成路径。JPEG 的外部透明区域合成到深色底。
- 快门显示 180 ms 白闪渐隐，240 ms 后保存已冻结的图像，闪光不写入照片；补充原创合成的 0.22 秒快门音，跟随系统音量，音量零不启动播放。反馈期间与结束后的短冷却抑制重复拍摄，声音进程有超时和退出回收。
- 初次截图发现分块刷新造成闪光分条，已改用既有整帧后台页面合成/翻页。后续十张连续截图逐帧右边缘 RGB 极差均不超过 1，覆盖高亮、渐隐与正常画面；这属于帧内容检查，不声称是高速摄影测得的屏幕扫描表现。
- 主机 ASan/UBSan 和 Pillow 测试通过：16 组画幅/相框、透明合成端点与空白窗口、素材缺失、快门计时/冷却/时钟回绕、原有裁切旋转及相册/写入失败检查。MIPS camera 构建通过。真机隔离数据验证三款相框、四种画幅、相册返回与连续三次空格只生成一张照片；正音量播放进程退出 0，音量零没有播放进程，最后恢复原音量 255/255。此项不代替用户听感验收。
- 本轮取景单款相框 RSS 约 5,932 KiB，包含拍摄的 HWM 16,552 KiB；选择器可缓存三张各约 1 MiB 工作纹理。近期 JPEG 编码/写盘耗时 267–511 ms，不包含前面的 RGB 转换及反馈等待，不代表长期性能。
- 首次上传在切换前 EOF 中断；恢复后重试部署成功。设备 uptime 与临时目录消失表明期间发生重启，原因未确定，不能仅归因于休眠或相机崩溃。正式 launcher 已打开新版相机并截图确认素材加载正常，随后停在相册，摄像头关闭。

证据保存在忽略目录 `.build/camera-feedback-qa/`。基线七张用户照片摘要完全相同，期间另外两张照片也保留，隔离 QA 没有向正式相册添加照片。设备临时文件及未激活的中断上传目录已清理；本地截图、测试、部署日志保留。USB-C 视频与 HID 只读调研见主项目 `docs/USB-C-VIDEO.md`，未修改 USB 组合或向电脑发送模拟键鼠输入。

---

# 2026-09-24：拍立得画幅、相纸与深色取景界面

已部署 `/storage/apps/releases/20260924-153938-80b1aeda`，134 个文件、56,179,368 字节，部署脚本逐文件 SHA-256 校验通过。与上一正式包比较，只有 `camera/c1max-camera`、camera manifest 和 catalog 内容发生变化，其他应用二进制保持一致。

- 按 ui-ux-pro-max 工作流审查并重新设计界面：左侧 568×340 深色取景台，取消白色底板和装饰镜头；右侧固定控制区，四个画幅按钮、滤镜/相纸选择层、快门和相册。默认 4:3、原色、无边框。相册与删除对话框使用同一深色配色；预览按比例适配，白/奶油相纸只包围实际照片。
- 四种画幅为 4:3、3:4、1:1、16:9，原色无框图片尺寸分别为 972×729、768×1024、972×972、960×540。通过统一中心裁切/滤镜/边框像素函数实现预览和保存一致；白相纸、奶油纸、黑胶片边框真实写进 JPEG。R/F/B 循环切换，触摸选择层支持 A/D、回车和返回，设置原子写入并在重启时恢复。
- 媒体图报告 ISP 输入 2048×1944。1024×972 与 1280×960 原始采集均约 15.04 fps，最终采用等比例的 1024×972，避免把此驱动报告的输入非均匀缩放成 640×480。仅保存最新完整 NV12 帧；预览直接转换所需采样点，快门时才分配完整 RGB。顺时针 90° 修正保留。未修改传感器寄存器或声称已开启完整 5 MP/光学广角/自动对焦。
- 主机 ASan/UBSan 原有帧转换、短帧、相册、删除和强制写失败测试通过；新测四种居中精确画幅、四种相纸、预览/保存取样一致性与新版横幅不误旋转。Pillow 独立解码 16 组画幅/相纸 JPEG，验证实际尺寸、中心颜色和相纸颜色。MIPS camera 目标构建与 `git diff --check` 通过。
- 真机隔离目录拍出四种画幅及全部相纸，真实 JPEG 经 Pillow 解码；Space、410 拍照键事件、回车、屏幕快门均成功保存。选择层触摸和 A/D+回车、返回相册/取景、删除取消/确认、退出并重开后保持 3:4+黑白+无边框均检查通过。输入为事件注入，不声称本轮由用户亲按键帽。
- 实测取景 RSS 约 3.7 MiB，包含拍摄的进程 HWM 10,936 KiB；这些进程数字不包含全部内核 ISP/mmap 驱动内存。取景时系统 MemAvailable 约 60 MiB。一次 10 秒等待窗口及启动/输入开销内收到 146 帧，正常退出状态 0；JPEG 编码/写盘日志约 230–457 ms（不含前面的 RGB 转换）。这些是短功能测试，不是耐久性或全部光照场景的性能保证。

证据在忽略目录 `.build/camera-format-qa/`，包括默认/宽幅/相纸选择/不同边框/黑白/相册/重开/正式入口截图、隔离 JPEG、测试与部署日志、采集探测和原图摘要。部分截图文件名沿用预定测试顺序，实际画幅以截图标签和 device.log 为准。用户正式照片安装前后 SHA-256 完全相同；隔离设备目录、探测帧及输入工具已清理。设备停留在正常 launcher 启动的新版拍立得取景页，未向正式相册添加测试照片。

---

# 2026-09-24：拍立得画面顺时针 90° 修正

已部署 `/storage/apps/releases/20260924-151059-9ab32ba4`，134 个文件、56,168,684 字节，部署脚本逐文件 SHA-256 校验通过。本轮开始时 ADB 未连接；用户唤醒并连接后完成真机测试和正式安装。

- V4L2 保持 640×480 NV12 采集；转换 RGB 时直接写到顺时针 90° 的位置，输出为 480×640，取景和新 JPEG 共用已旋正的像素，不额外分配全尺寸旋转缓存。预览 189×252、相册 202×270，保持完整 3:4 画幅，状态和分辨率标签同步更新。
- 旧版 640×480 且文件名符合本应用格式的照片在相册解码缩放时旋转，原文件不修改；新 480×640 JPEG 不再旋转。导入的其他 JPEG 保持原方向。
- ASan/UBSan 验证非方形像素矩阵顺时针排列，以及四象限样本从 NV12 转换、旧/新 JPEG 保存到相册解码后的方向；Pillow 验证新 JPEG 为 480×640、旧与导入原图仍为 640×480。既有相册/删除/保存/短帧测试通过。
- 真机隔离数据测试：取景截图、新拍 JPEG、相册中新/旧照片、返回取景均通过。对屏幕相册图像进行逐点抽样：旧图与原 JPEG 顺时针旋转后的理论结果 RGB 平均误差 0.060；新图与保存的竖向 JPEG 直接显示结果误差 0.041，差异符合解码舍入，排除了错误方向和重复旋转。
- 相册返回取景后连续获取 123 帧并正常退出；该次 HWM 3,984 KiB。未将这次短测当作长期帧率或所有光照场景验收。MIPS camera 目标构建、打包和 `git diff --check` 通过。

证据保存在忽略目录 `.build/camera-rotation-qa/`，包括 preview/new/old/live2/installed 截图、主机与设备日志、测试 JPEG 和用户原图摘要。设备隔离目录和临时输入/截图工具已清理，用户原图部署前后 SHA-256 相同；正式应用停留在相册，摄像头关闭。

---

# 2026-09-24：拍立得相册删除

部署 `/storage/apps/releases/20260924-021105-bf9bb2f4`，134 个文件、56,167,812 字节，部署脚本逐文件 SHA-256 校验后切换。

- 相册顶部新增「删除 X」，X 或右上退格键也可打开确认框。确认框显示当前文件名；点击删除或回车确认，点击取消或中间返回键取消。确认期间拦截其他翻页/拍摄键，电源仍直接回 launcher。文件删除后显示同一位置的下一张，删掉末尾则回前一张；唯一照片删除后进入空相册并禁用删除按钮。
- 删除限定于当前相册中被选中的普通 JPEG，检查设备/inode/修改时间，不接受越界路径、符号链接或同名替换文件。系统删除失败时保留当前相册条目并在确认框显示错误。确认键重复输入被抑制，避免关闭确认框后触发回取景或拍照。
- 真机仅在 `/tmp/c1max-camera-delete-qa` 使用测试 JPEG：取消后仍有三张；触摸确认删掉中间照片后 2/2；右上退格+回车删末尾后 1/1；X+回车删最后一张后 0/0。追加两张隔离样本，连续四次回车仅删除一张并保持相册 1/1。确认框、相邻照片和空相册均已截图检查。
- 主机 ASan/UBSan 覆盖删除成功、已不存在、同名替换、符号链接、越界路径与删空；原有相册解码、颜色转换及照片保存测试通过。MIPS camera 目标构建和 `git diff --check` 通过；补齐 LVGL image cache API 的显式头文件依赖。

证据在忽略目录 `.build/camera-delete-qa/`，设备隔离测试目录与输入工具已清理。正式入口验证了删除按钮显示，用户照片部署前后摘要完全相同；未在用户原图上执行删除，正式应用停留在相册。

---

# 2026-09-24：拍立得相册与空格拍照

部署 `/storage/apps/releases/20260924-015450-79589970`，134 个文件、56,162,556 字节，部署脚本逐文件 SHA-256 校验通过。正常 launcher 入口已验证；原有七张照片的部署前后 SHA-256 完全相同。

- 取景右上角新增相册入口，G 也可打开；按拍摄时间倒序排列，并优先打开当前会话刚拍的照片。相册完整按比例显示单张照片，A/D、左右键和触摸按钮翻页，边界按钮禁用。屏幕「返回拍摄」或中间返回键恢复取景；电源返回 launcher。
- 取景空格对应的 KEY_SPACE（57）注入两次，两张 JPEG 均为 640×480 且能完整解码；保留实体拍照键、回车和屏幕快门。底部显示空格提示。相册中的空格/回车/拍照键只返回取景，不会意外拍摄。
- 真机隔离目录验证空相册、两张照片的新旧翻页、触摸上一张/下一张、按钮/返回键恢复摄像头，以及损坏 JPEG 的错误提示和继续翻页。相册打开时内核报告 stream off，进程 fd 中无 `/dev/video4`；回到取景会重新获取帧。测试 HWM 4,016 KiB，相册 RSS 2,880 KiB，仅代表本轮 640×480 样本。
- JPEG 解码使用已固定 LVGL 依赖附带的 stb_image v2.30（仅 JPEG），同包带许可证。读取限制为 8 MiB、2048×2048，不跟随照片符号链接，不加载整个相册的像素。坏图先隐藏旧画面并展示错误，不删除或修改原图。
- 主机 ASan/UBSan 验证相册扫描/排序、空目录、无效目录、临时文件/子目录/符号链接过滤、横竖比例适配及截断 JPEG；原照片保存、强制写失败清理和 NV12 测试仍通过。完整 MIPS 构建和 `git diff --check` 通过。

证据在忽略目录 `.build/camera-album-qa/`：empty/latest/older/return/broken/installed 截图、两个空格拍摄 JPEG、主机与设备日志、用户原图摘要。设备隔离测试目录和输入工具已清理；正式应用停留在用户相册，摄像头关闭。

---

# 2026-09-24：拍立得真机修复

正式部署 `/storage/apps/releases/20260924-013147-3c8d461e`，133 个文件、56,125,323 字节，部署脚本逐文件 SHA-256 校验后切换。原 release、用户数据和正式相册保留；测试结束回到普通 launcher，并关闭摄像头。

- 旧版真机复现 `Camera produced a short NV12 frame`；`ispvideo` 的 640×480 NV12 报告 `bytesperline=960`、`sizeimage=460800`，实际紧密排列的 Y/UV 平面 stride 为 640。仅对该驱动和这组大小关系修正 stride，继续检查实际 payload 和 mmap 长度，不跳过短帧检查。
- `v4l2-ctl` 连续采集约 15.04 fps，预热后的帧能正常解码。应用原来的 1365/256 倍图像缩放覆盖了整个界面，已改为直接绘制 336×252 的 4:3 预览，完整构图与 640×480 JPEG 一致。已截图检查正式入口的预览、六个滤镜、快门和底部说明，无越界遮挡。
- 修复保存路径缺少 `camera/`；使用独立文件名、0600 权限、检查写入/flush/fsync 和完整文件发布，失败时清理临时文件。启动预热期间不允许拍摄；无有效帧超过四秒后释放摄像头并提供重试。
- 通过事件注入分别验证实体拍照键对应事件、回车、屏幕快门；F 和触摸按钮成功切换黑白/复古。三个真实 JPEG 均为 640×480，Pillow 完整解码且包含实际图像；黑白照片 RGB 均值相同。JPEG 编码器的 signed shift 被 UBSan 捕获，改为 unsigned 位缓存，最终二进制再次拍摄并解码成功。这里是事件注入验证，不声称用户亲按了键帽。
- 两轮连续预览分别收到 775 / 933 帧，电源返回状态码 0，重开可重新取景；内核对应输出 `ov5648 stream off`。最终二进制取景 RSS 约 2.9 MiB，拍照阶段 HWM 3,884 KiB。这不是长时间耐久性、所有光照条件或 5 MP/自动对焦功能验收。
- `bash camera/run-tests.sh` 的 ASan/UBSan、NV12/NV21 转换、带 padding 的 stride、逐字节短 buffer、无效尺寸/目录、唯一文件名和强制写失败清理通过；六种滤镜 JPEG 均可解码。完整 MIPS 构建及 `git diff --check` 通过。

证据保存在忽略目录 `.build/camera-qa/`（before/after/saved/installed 截图、V4L2 探测、真实测试照片、构建/测试/部署日志）。设备上的隔离目录 `/tmp/c1max-camera-qa` 和本轮输入/截图工具产物已清理；测试照片没有放进用户正式相册，也不打包或提交。

---

# 2026-09-24：StreamPlayer 本地字幕层

正式部署 `/storage/apps/releases/20260924-011033-1166b8e2`，133 个文件、56,095,635 字节，逐文件 SHA-256 校验通过。此版取代下面历史记录中的服务端烧录字幕：转码请求固定 `SubtitleStreamIndex=-1`，字幕独立获取并在本机合成。

- 修改前真机截图确认双语字幕已缩进视频像素，底部英文被屏幕截断。沿用用户暂停的同一集约 52:44 位置，修改后 Width 截图显示完整的大号中英文；控制条出现后字幕移到控制条上方，隐藏控制条后回到底部，保留 14 px 安全边距。
- 用户实际片源的中文/英文 PGS 均通过 Emby 独立 SUP 接口取得；本机解析调色板、对象片段、RLE 和显示/清除事件，保留原始字体、笔画与颜色，不做 OCR。切换中文→英文→Off 均能在暂停画面上生效，没有重启 MPlayer。英文与 Off 截图的 RGB 差异仅在字幕矩形 `(173,286)..(626,326)`，证明视频没有残留烧录字幕。
- A+ 从默认 32 调至 36，真实 PGS 截图变大；Fit 模式在暂停后跳转到约 52:54，保留暂停并显示完整中英文字幕。Width/Fit 的字幕采用同一屏幕坐标与尺寸限制，不参与视频裁切。
- 文字路径另外使用明确的测试字幕，在设备上通过同一绘制函数确认 Noto Sans SC、32 px、中英文双行、白字黑边和透明合成。此项是本地文字绘制验证，不声称已用用户片源验证服务器将 SRT/ASS 转为 WebVTT。复杂 ASS 排版、动画及 VobSub/DVB 未实现。
- 字幕后台按约 60 秒窗口预取，保留最多两个窗口；每个响应上限 1 MiB，PGS 仅解码当前字幕图片。测试阶段 120 秒密集双语样本 749,685 字节，最终缩短至 60 秒以降低瞬时内存和响应超限机会；最终窗口实际取得 13 条字幕。测试 GUI 峰值 RSS 6,276 KiB，MPlayer 主进程约 13,228 KiB，不能据此保证所有片源的峰值或长期性能。
- ASan/UBSan：VTT 时刻/重叠/标签、PGS 调色板/RLE/清除、逐字节截断/破坏输入、两种显示模式和控制条的安全区域通过。另有后台预取、缓存窗口、前后跳转、快速换轨取消、Off 和退出取消测试；两个真实 SUP 样本分别完整解码 23 / 30 条字幕。完整 MIPS 构建与 `git diff --check` 通过。

证据在忽略目录 `.build/subtitle-local-qa/`：`before.png`、`width.png`、`controls.png`、`large.png`、`english.png`、`off.png`、`fit.png`、`text.png`，以及测试与设备日志。播放测试使用隔离数据和 `-ao null`，没有验证声音/音画同步。正式包不含测试字幕、自动播放测试入口、影视内容或凭据；用户登录配置保持不变。过程中 ADB 曾断开，用户唤醒设备后继续完成真机测试。

---

# 2026-09-24 凌晨：StreamPlayer 分段切换、字幕与 Terminal 字号

正式部署 `/storage/apps/releases/20260924-000008-e298d110`，133 个文件、56,035,195 字节，逐文件 SHA-256 校验后原子切换。旧 release 和用户数据保留；StreamPlayer 登录配置部署前后 SHA-256 相同。用户 AFK 并授权结束设备进程，测试结束后已停止测试播放器并启动正式 launcher。本轮未继续修改 FF7；用户报告的无声问题仍未解决，下面的静音视频验证也不代表音频验收。

- StreamPlayer 恢复 CardputerZero 风格的深色登录、Emby 绿 / Jellyfin 蓝、左侧媒体库、三张封面卡片和详情页，适配 800×340；正式入口使用用户原服务器地址成功打开媒体库，截图检查通过。
- 应用读取 HLS VOD 清单，完整 MPEG-TS 分段经有界 FIFO 交给同一 MPlayer。明确指定 lavf/mpegts 和受限探测窗口，解决原厂自动 TS 探测等待过多数据、与预取限制互相等待的问题。新段请求失败重试一次；退出可取消正在进行的下载。
- 真机同一影片 Width→Fit→Width 成功，日志确认解码帧从 400×224 变为 302×170，再回到 400×224；新尺寸完整帧到达后才切本地布局。请求边界分别为 400×288 / 400×170，保持源比例，不声称服务器已经裁切为屏幕比例。
- 字幕列表获取四条中文 PGS 轨道，可关闭/切换；同一 MPlayer 进程在分段边界从 Off 切到轨道 3。随后跳转至约 1:52，Fit 真机截图明确显示中文字幕，和独立下载的服务端转码片段一致。轨道编号用于区分同名字幕；Width 裁切会影响底部字幕，建议字幕使用 Fit。
- 分段期间连续播放超过七分钟；观察约 117 帧 / 5 秒，服务端实际输出 23.976 fps，未完全遵从请求的 20 fps。暂停两秒的两次 framebuffer 捕获逐字节相同；暂停后跳转继续保持暂停 25 秒以上，恢复后帧数与进度持续增加。字幕切换不重启播放器，主动跳转会重启。
- 原厂 MPlayer 丢弃输入包 PTS，无法依靠解码帧时间戳；使用 slave 时间线和帧尺寸切换布局。真实 FFmpeg ABI 检查、单一 framebuffer 所有者与完整帧交付保持。慢网络/字幕烧录启动仍可能短暂停顿，不能把本轮结果解释成所有视频无缝切换或音画同步通过。
- Terminal 真机 `top -d 1` 中注入 Shift+音量键：16→18→20→18→16，网格 80×14→72×12→66×11→72×12→80×14，程序接收 PTY 尺寸变化并重排。组合键前后 ALSA softvolume 都为 255/255；没有新增界面提示。正式入口与电源返回通过。此为输入事件注入，不冒充用户亲按实体键验收。
- 完整 MIPS 构建通过；StreamPlayer 时间线、HLS 解析/边界/拒绝非法格式、碎片帧与尺寸变化、字幕面板合成、键盘映射、Terminal ANSI/UTF-8/历史/resize、真实 PTY 尺寸/中断/退出测试通过 ASan/UBSan。`git diff --check` 通过。

视频 QA 使用 `C1_STREAMPLAYER_SILENT=1` / `-ao null`，没有验证发声或音画同步；正式 launcher 没有保留静音环境变量。设备 DNS 曾间歇失败，部分持续播放验证使用隔离配置中的同一 Emby 服务器 IP，正式用户地址没有被替换；部署后原地址的媒体库请求再次成功。临时代理已关闭、ADB 转发已移除、测试配置与令牌副本已清理。证据位于忽略目录 `.build/stream-switch-probe/`。Jellyfin 独立服务器、加密 HLS、fMP4、动态直播及 discontinuity 不在本轮已验证范围。

---

# 2026-09-23 晚：PCSX 音频与 FF7 黑屏诊断

已部署 `/storage/apps/releases/20260923-223242-37c4e20e`（133 个文件，55,407,407 字节），逐文件 SHA-256 校验通过，旧 release、游戏镜像及存档保留。设备设置为 `interpreter=false`、`muted=false`，即默认 MIPS dynarec 快速模式和有声模式。

- 原先 tinyalsa PCM 在模拟器主线程同步写入；真机对比时，开启声音约 17 FPS，静音约 60 FPS。现在 PCM 写入走独立线程和有界队列，忙闲状态由声卡缓冲占用反馈给 SPU，过载时丢弃积压音频，不阻塞画面线程。
- 新版真机测试成功打开扬声器路由和 44.1 kHz PCM（1280×8 period）。自写 PS-X 测试程序及 FF7 镜像的 headless 测试均稳定约 60 FPS；FF7 启动采样约 13.7–14.4% CPU。音频队列记录到一次过载，诊断构建统计提交 268,456 帧、写入 265,150 帧，其余包含队列过载及测试结束时尚未播放的块；这不是听感验收。
- FF7 启动采样在 VBlank 2 主动关闭 GPU 显示，持续至少 5,950 ms，360 帧测试结束时仍处于 blanking。由此确认黑屏期间游戏本身设置了 GPU blanking；加载完成后由游戏重新打开显示。新版日志会记录 `C1MAX_PSX_DISPLAY_BLANK` 和 `C1MAX_PSX_DISPLAY_RESTORED blank_ms=...`。本轮没有输入到战斗，战斗转换的具体时长仍待实测；MIPS dynarec + Unai 软件渲染仍是可能的额外负担。
- 更新检查访问公开仓库的 11 项清单，结果均为同版本源码哈希不同；此前 UI 把这种方向不明的情况统计为“更新”。现已改成显示 `source differs`，只有远端版本更高或新增项才计为更新。该页面仅读清单，不下载或覆盖应用。
- 音频 smoke 结束后通过 `alsactl` 恢复 launcher 保存的混音器状态，复核扬声器配置回到 `Off` / `ZERO`；用户原 launcher 已重新启动。未改写用户设置、ROM 或正式存档。

---

# 2026-09-23 下午：PS1 原始分辨率与 ZIP 游戏包

已激活 `/storage/apps/releases/20260923-160736-ea7e2920`（114 个文件、44,893,391 字节），安装前完整 SHA-256 校验，保留旧 release 和用户游戏／存档。PCSX4all 0.1.1、DOSBox 0.1.2。先前用户要求等待游戏退出的条件已满足；部署没有打断其原先的游戏。

- PS1：原生 VRAM 直接映射至 453×340 或 800×340，取消上游固定 320×240 中间画面，并禁用 GPU 跳列／高分辨率跳行。15-bit 与 24-bit 色彩、VRAM 环回、垂直黑边、非法几何保护在 ASan/UBSan 和真机 MIPS 单测通过；640 宽奇数列细线与 480 行奇数扫描线不再预先丢弃。
- 两侧中文实体键提示在三页 framebuffer 上静态缓存，菜单与游戏切换没有覆盖游戏区域；拍照切换全宽／原比例，暂停菜单也可选择，设置在退出并重开后保留。字体复用 Noto Sans SC，生成侧栏后释放字体映射和字形缓存。
- 用户《北欧女神》Disc-1.cue，HLE＋解释器＋静音：汉化启动菜单按 K（○）继续，经过启动标志到 New Game/Continue 主菜单；实际记录源分辨率依次 320×240、512×240、512×480。两种显示模式、返回暂停已验；旧版本短按电源回 launcher 的行为已验，新增长按五秒退出规则待真机复测。本轮峰值 6400 KiB。此前“卡住”的具体现场无法复原，不能据此保证后续流程或 HLE 兼容性。高分辨率 GPU 绘制开销会增加，未测长时间帧率或音画同步。
- DOS ZIP：中文路径压缩包中的原创 BAT 真机执行，独立 `.pure.zip` 内 `RESULT.TXT` 精确为 `ZIP_OK\r\n`，退出码 0，峰值 35280 KiB。散装 COM 与 ZIP 加载路径分开；没有启用虚拟键盘或音频。960 MB 的用户 ZIP 包在真机完成 60 帧启动菜单测试，峰值 34016 KiB；这不代表进入游戏后的内存或速度。
- DOS 两侧提示：4:3 为 453×340，16:9 为 604×340，后者每侧留 98 px；默认 4:3，DOS shell 沿用设置。真机验证两种画幅、游戏/文本提示切换、双击 Shift 后“大写”状态、NAV 前缀说明；不再把临时提示条画在游戏底部。点击侧栏前后截图 SHA-256 相同，帧缓存和两种画幅的触摸边界 MIPS 单测通过。LAB 本轮峰值 34480 KiB，正常电源返回。
- NAS：49 个 ZIP 游戏与原有 metadata/media 共 197 个文件、3,054,096,628 字节，NAS→主机→设备 SHA-256 全部一致，全部 ZIP 额外通过 CRC 检查。存放于 `/storage/apps/data/dosbox/games/NAS-DOS/`，在临时目录校验后原子移入；游戏库显示 50 项（含内置 LAB）。未修改 NAS 原文件。《仙剑奇侠传》ZIP 内选择 `PAL\PAL.EXE`，实际到达山水标题画面；未继续测试完整游玩、游戏内存档或声音。包中还含安装器、杀毒工具等附属程序，不能把任意 EXE 当成游戏入口。
- 用户此前新增 PS1 5 张光盘均已从 CHD 校验转换为 BIN/CUE，10 个文件共 3,630,521,673 字节；设备 SHA-256 与主机一致。Valkyrie-Profile 两盘，Final-Fantasy-7-Hack 三盘。未实现运行中换盘，未验证多盘推进；镜像及商业游戏截图仅保存在忽略目录和用户设备。
- ADB 断连诊断：本次日志为 15:41:28 系统 suspend、15:44:44 resume，开机约 6 小时，没有本次重启证据。`sys.backlight.timer=1200000`、`sys.suspend.timer=15000`；背光关闭至 suspend 的日志间隔也约 16 秒。提前息屏的触发者未确定，没有擅改原厂电源设置。

全部测试静音，softvolume=0,0。证据保存在 `.build/qa/pcsx4all/new-games/`、`.build/qa/dosbox/flicker/`、`.build/qa/dosbox/nas-import/`，不把商业 ROM、截图、NAS 原文件、设备设置数据库或凭据纳入 Git。

---

# 2026-09-23 DOSBox 0.1.1 闪烁修复

用户反馈内置 DOS LAB 闪烁。确认不是只凭单帧截图猜测：记录 DOSBox 的原始视频回调，真机旧版 210 帧中 48 帧缺少文字或方块；修改后 210 帧全部保留 50 个采样绿色像素、50 个黄色像素及 768 个文字像素。

原因是原创 COM 每轮清空 VGA 显存再画，模拟的 VGA 刷新会捕获清屏中的内容。现在初始化清屏一次，只重绘移动方块和改变的分数；前端也缓存像素、画幅及提示条，完全相同的画面不再重复翻页。菜单覆盖、画幅切换与提示变化仍强制重绘；PAN 失败不会把画面错误标记为已提交。画面缓存的行填充、像素变化、分辨率变化、非法输入保护通过独立 MIPS/QEMU 测试。

新示例 649 字节，SHA-256 `72939a2e74266bb006237bea5fcd0b94a8ccc96fecfefdb9f011215c778daf46`。确认设备旧文件匹配原版 SHA-256 后，备份为 `C1LAB.COM.0.1.0.bak` 再替换，保留 `RESULT.DAT`。用户自行退出后，已随新 release 正式安装（见下方最新记录）。DOS LAB 继续运行、移动、暂停/恢复及电源返回通过，110 秒运行正常退出、峰值 33288 KiB；这期间截图有位置和分数变化，不能把它们作为无人输入时像素完全不变的验收证据。原始回调稳定性与缓存逻辑有上面的独立测试。

证据：`.build/qa/dosbox/flicker/`。测试保持静音。此结论针对已确认的 DOS LAB 清屏闪烁；不能以此保证所有 DOS 游戏或面板显示问题都已消除。

---

# 2026-09-23 DOSBox 与用户 CHD 导入

正式部署：`/storage/apps/releases/20260923-152053-58e9a81e`，114 个文件、约 44.8 MB；逐文件 SHA-256 校验后原子切换，保留旧 release 和用户数据。Launcher 共 11 个入口，第二页 DOSBox 为 E。

DOSBox 0.1.0：DOSBox Pure 固定提交 `73e03aa145e0549ed4d5a20f8e65532714da33f5`，轻量原生前端，无 RetroArch/虚拟键盘。解释器模式，默认 8 MB DOS 内存与静音。修正 MIPSel 未对齐访问；动态重编译仍在真机崩溃，因此发布构建明确禁用。未采用已不受上游支持的 partial TLB 优化。

- 原创 VGA DOS LAB 真机运行；键盘 D 移动、触摸移动并收集目标，分数变成 1。Esc 写出 7 字节 `RESULT.DAT`，内容为 score=1、x=248、y=125、lastkey=0x011b；正常退出回游戏库。
- 图形模式（8 MB）运行约一分钟并测试暂停/继续，峰值 32672 KiB；80 列 DOS shell 峰值 35216 KiB。16 MB 内存配置在真机完成 180 帧，峰值 41060 KiB，约 2573 ms。均不能代表大型游戏内存或性能。
- 文本模式事件注入验证双击 Shift 大写、Shift 数字/冒号/斜杠/句点、退格、回车、符号前缀及 DOS 重定向。`KEYS.TXT` 精确字节 `41 5a 31 3a 2f 2e 0d 0a`（`AZ1:/.` + CRLF）。发现并修复 `<` / `>` 映射颠倒。
- ASan/UBSan：按住/释放、跨 Shift 释放、大小写、方向/F1–F12/Ctrl/Alt/符号前缀、游戏模式与音量键不消耗前缀通过。实体键帽码仍沿用已约定映射，本轮为事件注入，不冒充用户亲按验证。
- `.bat` 小写扩展名实际启动、重定向写文件通过，输出 `C1DOS_OK`；修正快速结束且未产生画面的脚本被误报退出码 1，复测退出码 0。
- 此前版本按电源从核心回 launcher，无残留 DOS 核心；新增长按五秒退出的规则仍待真机验证。显示通过单一 framebuffer 所有者、离屏页整体提交；暂停菜单切换模式/画幅，默认 4:3，DOS shell 铺满宽度。包内原创 COM 由 NASM 构建，没有打包商业游戏。
- DOS 图标为 imagegen 生成的 RGBA 1254×1254，四角 alpha=0，817145 个完全透明像素；打包保持比例缩至 96×96。暂停菜单无 Exit 按钮，长按拍照键开启。

用户 CHD 已通过 chdman SHA-1 校验并无损解压为 BIN/CUE，设备文件 SHA-256 与本地一致。ISO 标识 `IMBNES`、启动项 `NES.EXE`，含六款 NES 洛克人 HACK；它不是原生 PS1《洛克人传奇》。放于 `/storage/apps/data/pcsx4all/roms/Rockman-Hack/`，选择 `Rockman-Hack.cue`。HLE＋兼容模式确认合集菜单与第一款的选关画面；保留兼容模式及静音。镜像、截图、临时日志只在本机忽略目录和用户设备，不上传 ROM/BIOS。

全程 softvolume=0,0，DOS/PCSX 均静音，未测试发声、音画同步或长期游戏稳定性。DOS 当前已验证原创 VGA 程序、shell、鼠标、普通文件保存；复杂商业游戏、保护模式、3D、Windows 与即时存档不在本次已验证范围。证据在 `.build/qa/dosbox/`、`.build/qa/pcsx4all/chd-convert/`。

---

# 2026-09-23 新应用与公开更新源

当前发布：`/storage/apps/releases/20260923-123400-1ab7acf4`，共 103 个文件、约 38.4 MB，保留上个 release 和全部用户数据。应用目录已独立为公开仓库 `zhuzhe1983/C1Max-Apps`，主项目通过 Git submodule 引用。以下测试继续遵守静音要求，softvolume 确认为 `0,0`，PCSX 核心使用 `--mute`，没有测试发声或音画同步。

- Calendar 0.3.0：迁入 CardputerZero 的 7 项内置外部源，默认关闭、按 ID/URL 去重、保留自定义订阅。中国节假日与黄历在词典上可抓取；其余 5 个 Google 源在本次网络下超时，未绕过网络/证书检查。日历模型与持久化回归通过，测试订阅和用户数据分离。
- Gomoku 0.1.0：15×15 自由规则，人机/双人、悔棋、原子保存与恢复。ASan/UBSan 规则、四个方向获胜、AI 即胜/防守、损坏存档与完整对局测试通过；真机落子→AI 回应→悔棋→重开续局验证通过。
- PCSX4all 0.1.0：MIPS 重编译和解释器各通过自写 PS-X EXE 的 180 次垂直同步测试，最新动态模式 RSS 峰值 19744 KiB，解释器 5384 KiB。真机可见运动三角形；暂停菜单、即时存档、读档继续、返回游戏库和电源返回通过。约 68 KB 的即时存档写入隔离目录；错误镜像能返回游戏库并显示失败提示。正式游戏目录为空，没有打包商业 ROM 或 BIOS。不能据此推断商业 3D 游戏的性能或兼容性。
- Processing 2D 0.1.0：QuickJS + Processing 风格 JavaScript 子集，不是官方 Java Processing/p5.js。四个官方示例改编各跑 180 帧，包含 Koch 第 5 级；测试进程峰值 2564 KiB，完整 GUI 历经示例、编辑与重开后峰值 3368 KiB。无限循环、堆超限、错误后恢复、裁剪、alpha 混色和触摸按下边沿测试通过。
- Processing 真机：四项示例、暂停/继续、物理键盘大写 `A`、相机前缀 `{`、右上退格、保存与退出后重开通过，保存文件与预期 SHA-256 相同。语法错误可回编辑修正。发现并修复放大图像命中框仍为原尺寸的问题；点击逻辑 `(680,220)` 后截图同坐标为预期 RGB `(255,150,80)`。仍是事件注入测试，真实键帽码采样的历史待验收项不因此自动关闭。
- Launcher：正式 10 个入口分两页；新 PCSX4all/Processing 图标为真实 alpha，缩放保持比例。两个正式入口均启动，PCSX 空游戏库显示导入路径，Processing 正式包群聚示例可运行。所有 Power 返回沿用监督器恢复流程。

源码依赖固定提交或 SHA-256；首次 Git 依赖抓取显式 checkout，避免 `clone --no-checkout` 恰好位于目标提交时留下空目录。QuickJS 导出头文件隔离于 `VERSION`，避免 macOS 大小写不敏感目录覆盖 C++ `<version>`。完整 MIPS 构建、打包脚本语法、部署校验通过。证据保存在忽略目录 `.build/qa/processing/`、`.build/qa/pcsx4all/`、`.build/qa/gomoku/` 和部署日志。

仍待用户数据或独立环境验证：PS1 商业游戏/BIOS/光盘音轨/3D 帧率，声音及音画同步，Google 日历源在设备网络下的连通性。Processing 仅支持 README 列出的 2D API；电子书、2048、B 站和 EasyRPG 尚未移植。

---

# 2026-09-23 上午前版真机验证（历史）

设备 MagicPen-931f06，原装 MPlayer 1.4 / FFmpeg 4.2，340×800 旋转 framebuffer，系统报告总内存 103112 KiB。用户已连接设备并授权部署。本轮最终视频验证使用 `C1_STREAMPLAYER_SILENT=1` / `-ao null`；兼容性探测和后续播放期间 softvolume 均为 0；收到工作时间要求后未测试钢琴发声、音量增大或其他音频功能。

最终发布目录：`/storage/apps/releases/20260923-101936-03b79027`。主机安装包：`apps/.build/c1max-apps-0.2.0-preview.tar.gz`；部署时校验全部 67 个文件。

## 已部署并验证

- 日历：通过隔离目录中的新增、日期输入、退格、编辑、保存、退出后新进程重载、再次修改、确认删除与电源返回；没有写入用户日程。
- 终端：真实 PTY 与 Bash 键盘输入 `printf`、引号/反斜杠/管道、输出到文件，核验隔离 HOME，退出 shell 后应用保留提示，再按电源回 launcher；截图文字正确。
- Launcher：临时 9 项配置验证第 2 页、键盘与横向滑动翻页、末页空位无动作；正式配置仍为 7 项。新图标已查看真机截图。
- StreamPlayer 输入：双击 Shift 后实际输入大写 C/A；退格令测试密码从 3 字符变成 2 字符，中间返回不删除，Enter 从服务器框切到账户框。未提交测试密码、未覆盖登录信息。
- 快捷入口：按 event0 Shift + event1 Enter 保持 1.5 秒以上再松开可进入最终版本；热键服务已重启并确认加载最终发布目录。
- 电源返回：子应用→launcher→原厂 smartUI 均正常；自定义界面运行时原厂 smartUI 已停止，可用内存从约 35 MB 提升至 70 MB。
- 静态 MIPS 的 launcher/API/钢琴离屏渲染/计算器自测通过；计算器表达式 `(12.5+3.5)*2` 返回 32。Bash、less、nano、dbclient 版本可在设备执行，Bash 数组/算术通过。

## 视频实际发现与修复

原厂 `-vo help` 没有 yuv4mpeg；PNG 虽列在输出列表，设备 libavcodec 没有 PNG encoder，不能用于播放。这两条路径的通用电脑测试不能代替原厂能力检测，失败的 PNG 路径已从最终代码移除。

新后端只对原厂 MPlayer 子进程加载 NaN2008/O32/FP64 的 `c1max-yuv-pipe.so`，取完整 YUV420 帧并用 `-vo null`；应用单独合成视频与 LVGL 控件，只有应用访问 framebuffer。不改系统 MPlayer 或动态库。适配器严格检查 libavutil 56.31.100，错误格式/尺寸/断管明确失败。

- 实际 Emby 登录会话、6 个媒体库与影片列表可用。静音连续播放数分钟，稳态每 5 秒约 98 帧（约 19.6 fps）；播放时可用内存约 59 MB；10 秒采样中应用与播放器合计占全机 CPU 容量约 14%。
- 控件显隐、默认填宽裁切、Fit 切换已截图验证；暂停 18 秒的两个显示截图逐像素一致，恢复后帧数与时间继续增长。
- 发现并修复“暂停后跳转变成播放”：旧帧/时间在异步请求期间触发了无效 pause 并清除待暂停标志。现在仅在新解码器获得时间和完整帧后执行待暂停；同时不再让过期 ANS_pause 回写用户意图。
- 修复后跳转 +10 秒仍保持暂停，持续超过 50 秒的帧计数与时间不变；恢复播放、在跳转缓冲期间按播放以及触摸进度条跳转均通过。
- 已通过电源退出后确认无残留 MPlayer，原厂界面恢复。

真机证据位于忽略目录 `apps/.build/qa/20260923/`，包括密码输入、日历 CRUD、终端输出、9 项分页、不同视频状态与构建记录。应用日志只记录帧计数/进度/暂停及固定错误码，不含播放 URL。最终设备已回到原厂桌面，音量为 0，无残留播放器/FIFO/播放列表，67 个发布文件校验通过，临时隔离测试目录已删除。

## 电脑回归与剩余范围

完整帧/碎片输入/时间线/裁切旋转/边界、键盘状态机、热键、日历持久化、终端 PTY/UTF-8 的已有回归通过。新的 YUV hook 用真实 FFmpeg 4.2 解码 20 帧 H.264，与独立 `av_image_copy_to_buffer` 逐字节一致；FIFO 断开能报告错误且不被 SIGPIPE 终止。编译产物与部署包均校验 SHA-256。

尚未验证：用户实际按右侧三个实体键与音量键的键帽对应；有声播放及音画同步（按本轮要求禁用）；Jellyfin 独立服务器；在线 ICS 订阅；具体 NES ROM；实际 SSH 远程登录。decode hook 在原厂本帧展示等待前取帧，稳态可能提前约一帧，静音流畅不代表音画同步通过。面板主观闪烁仍需要用户实际观看确认，静态截图不替代该确认。

---

# 2026-09-22 前版实机记录（历史）

2026-09-22，MagicPen-931f06，Ingenic X2000 / MIPS32r2，Linux 4.4.94，128 MB RAM，800×340。

- 六个 launcher 入口、日历月份切换、计算器触摸及物理 Shift 数字输入已在设备验证。日历与计算器模型测试通过。
- 钢琴修复白键重绘覆盖黑键；24 键、多键保持和三帧旋转渲染回归通过，设备截图确认黑键保留。
- Emby 实际账户登录、六个媒体库、服务器 H.264/AAC 转码播放通过。原厂 HLS 打开失败，改用连续 MPEG-TS 端点。
- 视频默认按宽度缩放，居中裁切；Fit 模式保留完整画面。暂停/继续、拖动进度、前后跳转、跳转后保持暂停、音量按钮及物理音量键已验证。
- 修复播放器把虚拟高度从 2400 改成 800 导致的冻结。视频写第 0 页，完整合成后交替显示第 1/2 页，避免扫描中的画面被部分覆盖；截图确认控件完整、继续播放后视频内容推进、控制条自动隐藏。
- 最终版本暂停时分别捕获 offset 800 和 1600 的显示页，1,088,000 字节逐字节一致（差异 0）；恢复播放与 Fit 重开流后重新验证画面，未复现先前的半幅控件覆盖。
- TS 起始时间、暂停、前后跳转、非法时间值、时长限制与 64 位溢出回归通过 ASan/UBSan 检查。
- 播放及控制条占用下可用内存约 49 MB；停止并退出后无残留 MPlayer，Emby 会话不再显示正在播放，原厂桌面与进入前混音器状态恢复。
- GitHub 检查通过设备 HTTPS 实际请求，当前返回“更新源私有或未发布”；没有误报已是最新版。清单版本、修订、非法 ID 和 URL 校验已通过。
- 全局快捷入口：短按 Shift+Enter 不启动，保持至少 1.5 秒并松开后打开 launcher；退出恢复原装桌面。热键状态机与真实/陈旧前台锁的回归通过 ASan/UBSan。安装独立 init 服务后完整重启，`smartUI` 与 `c1apps-hotkey` 均为 running，root ADB 自动恢复。

Jellyfin 仅完成兼容 API 适配，尚未用独立服务器联测。NES 尚无用户 ROM，未验证具体游戏兼容性。未公开仓库、未上传凭据或自动安装远程应用。
