# C1 Max 物理键盘

右侧三个实体键按用户照片与实际反馈定义：右上小方键 **Backspace / 退格**，中间长键 **返回上一级**，右下小方键 **Enter / 回车**。所有自定义应用短按 **电源键回 launcher**；launcher 中短按电源回原装桌面。没有屏幕 Exit 按钮。

本轮把 code 111 改为退格、code 14 改为返回、code 28 保留回车、code 116 独立为 Home。此前文档把右上与中间键的码对应写反，用户反馈已推翻此前说明。这次修改根据实际错误行为推断；2026-09-23 已在真机用事件注入确认退格删除、返回不删除、回车切换输入框、双击 Shift 输入大写；用户暂不便配合实际按键，因此键帽与驱动事件码的对应仍待实体采样确认。event0/event1 的共享输入按时间合并，避免不同设备的 Shift 释放先于字母被处理。

## 大小写和符号

**快速双击 Shift 切换小写 / 大写**，每次轻按不超过 350 ms，两次松开间隔不超过 350 ms。再次双击恢复小写。Shift 与字母组合不算双击，重复按住不会误切换大小写。每次进入应用默认小写；输入界面用 `abc` / `ABC` 或 `CAPS` 显示状态。

Shift 加字母仍然输入键帽数字和符号，大写状态下也是如此：

| 按键 | Shift 后 |
| --- | --- |
| Q W E R T Y U I O P | 1 2 3 4 5 6 7 8 9 0 |
| A S D F G H J K L | ~ @ # $ % & * ( ) |
| Z X C V B N M | . - / ? : ; , |

例如大写 `A`：双击 Shift 后按 A。输入 `http://`：普通字母、Shift+B 冒号、Shift+C 两次斜杠。原装系统中的输入方式不受这个自定义应用输入层影响。

输入层不创建屏幕虚拟键盘。StreamPlayer 点击输入框直接打字，Enter 切换下一框，右上退格删除，中间返回结束输入而不删除内容。列表 J/K 或空格移动选择，Enter 确认。播放时 Enter/空格暂停，A/D 前后 10 秒，Q/E 前后 1 分钟，H 显隐控制条，F 切换填宽/完整画面，V 静音；中间返回停止播放，电源直接回应用菜单。

## 各应用

- Launcher：每页 8 项，Q/W/E/R/T/Y/U/I 打开当前页对应位置；A/D 或方向键、左右滑动翻页；Enter 打开当前选择，电源回原厂。末页空位不响应。
- 日历：WASD 选日、Q/E 翻月、T 今日；日程与订阅管理提供实体键输入、保存/取消、返回上一级，详见日历 README。
- 计算器：Shift 数字、A 加号、Enter 计算；右上退格修改表达式，电源回菜单。
- 钢琴：A/W/S/E/D/F/T/G/Y/H/U/J/K/O/L/P/Z/X/C/V/B/N/M/Q 两个八度半音；支持多键，电源回菜单。
- NES：WASD 方向、J/K A/B、Q 选择、E/Enter 开始；电源回菜单。触摸游戏控件保留。
- 五子棋：WASD 选位置、回车/空格落子，U 悔棋、N 新局、M 切换人机/双人。
- PCSX4all：WASD 方向、J/K/U/I 为 ×/○/□/△，Q/E 和 Z/C 为肩键，回车 Start、空格 Select，中间返回打开存档菜单；电源回 launcher。
- Processing：Q/W/E/R/T 选示例或自己的程序；运行中 P/空格暂停、R 重置、E 编辑；编辑器相机键前缀提供导航/额外标点，返回保存，电源保存并回 launcher。
- Terminal：真实 PTY；返回发送 Escape，右上退格发送 DEL。符号前缀提供 Ctrl、方向、Tab 和额外标点，详见 [终端说明](terminal/README.md)。

音量由 launcher 会话内唯一 `c1max-volume` 处理，减/加每次调整 softvolume 13/255，支持长按，所有自定义应用共享。退出回原厂时恢复进入前的混音器状态。

原装系统进入 launcher：已安装的入口为 **Shift＋右下角回车保持至少 1.5 秒，再松开两个键**。短按不触发；原厂普通按键处理保留。未修改厂商桌面二进制。
